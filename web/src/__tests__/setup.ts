/**
 * Test setup for Bun test runner
 *
 * Note: Smoke tests use file system checks rather than dynamic imports
 * to avoid dependency resolution issues. This setup is minimal.
 */

// No global setup needed for file-based tests
// This file is loaded via bunfig.toml preload
