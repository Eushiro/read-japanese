# Japanese Reader API Configuration
# Copy this file to .env and modify as needed

# Environment: development or production
ENVIRONMENT=development

# Server configuration
API_HOST=0.0.0.0
API_PORT=8000

# Number of workers (only used in production mode)
WORKERS=4

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# CORS: Comma-separated list of allowed origins
# Use * for development, specific domains for production
# Example: https://your-app.com,https://www.your-app.com
ALLOWED_ORIGINS=*

# Rate limiting: requests per time period
# Format: number/period (e.g., 100/minute, 1000/hour)
RATE_LIMIT=100/minute

# Eleven Labs API key for TTS audio generation (optional)
# ELEVEN_LABS_API_KEY=your_api_key_here

# OpenAI API key for story and image generation (optional)
# Required for /api/generate endpoints
# OPENAI_API_KEY=sk-your_api_key_here
