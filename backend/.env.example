# Japanese Reader API Configuration
# Copy this file to .env and modify as needed

# Environment: development or production
ENVIRONMENT=development

# Server configuration
API_HOST=0.0.0.0
API_PORT=8000

# Number of workers (only used in production mode)
WORKERS=4

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# CORS: Comma-separated list of allowed origins
# Use * for development, specific domains for production
# Example: https://your-app.com,https://www.your-app.com
ALLOWED_ORIGINS=*

# Rate limiting: requests per time period
# Format: number/period (e.g., 100/minute, 1000/hour)
RATE_LIMIT=100/minute

# OpenRouter API key for story generation (required for generation)
# Uses free models like xiaomi/mimo-v2-flash:free
# Get from: https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-v1-your_api_key_here

# Text model for story/description generation (optional, has default)
# Default: xiaomi/mimo-v2-flash:free
# TEXT_MODEL=xiaomi/mimo-v2-flash:free

# Google AI API key for Gemini image generation
# Get from: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your_api_key_here

# Image generation model (optional, has default)
# Default: gemini-2.5-flash-image
# IMAGE_MODEL=gemini-2.5-flash-image
