# Japanese Reader API - Production Configuration
# Copy this file to .env when deploying to production

# Environment
ENVIRONMENT=production

# Server configuration
API_HOST=0.0.0.0
API_PORT=8000

# Number of gunicorn workers (recommended: 2 * CPU cores + 1)
WORKERS=4

# Logging level for production
LOG_LEVEL=WARNING

# CORS: Set your actual domain(s) here
# IMPORTANT: Do not use * in production!
ALLOWED_ORIGINS=https://your-app.com,https://api.your-app.com

# Rate limiting (adjust based on expected traffic)
# More restrictive in production to prevent abuse
RATE_LIMIT=60/minute

# Eleven Labs API key (if using TTS)
# ELEVEN_LABS_API_KEY=your_production_api_key_here
